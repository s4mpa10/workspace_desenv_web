// Definição de uma variável constante(const) e que não pode ser mais modificada durante a execução do código
const a = 3;

// Com esse comando abaixo vai da erro:
// a = a + 10;

//Dessa forma, é mais saúdavel para o programa ter mais constante do que variáveis, adicionando assim variáveis que possuem o mesmo valor durante toda a execução do código


console.log(a);
