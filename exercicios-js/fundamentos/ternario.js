// Operador ternário é aquele que possui três operadores
const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado';
//Return fica implicito, está simulando o IF e ELSE

console.log(resultado(7.1));
console.log(resultado(6.7));